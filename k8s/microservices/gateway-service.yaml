---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: neo4flix
  labels:
    app: gateway-service
spec:
  type: NodePort
  ports:
    - port: 9080
      targetPort: 9080
      nodePort: 30080
      name: http
  selector:
    app: gateway-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: neo4flix
  labels:
    app: gateway-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
        - name: gateway-service
          image: neo4flix/gateway-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9080
              name: http
          envFrom:
            - configMapRef:
                name: neo4flix-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9080
            initialDelaySeconds: 30
            periodSeconds: 5
