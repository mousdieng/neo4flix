---
apiVersion: v1
kind: Service
metadata:
  name: movie-service
  namespace: neo4flix
  labels:
    app: movie-service
spec:
  type: ClusterIP
  ports:
    - port: 9081
      targetPort: 9081
      name: http
  selector:
    app: movie-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-service
  namespace: neo4flix
  labels:
    app: movie-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: movie-service
  template:
    metadata:
      labels:
        app: movie-service
    spec:
      containers:
        - name: movie-service
          image: neo4flix/movie-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9081
              name: http
          envFrom:
            - configMapRef:
                name: neo4flix-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9081
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9081
            initialDelaySeconds: 30
            periodSeconds: 5
