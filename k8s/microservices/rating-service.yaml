---
apiVersion: v1
kind: Service
metadata:
  name: rating-service
  namespace: neo4flix
  labels:
    app: rating-service
spec:
  type: ClusterIP
  ports:
    - port: 9082
      targetPort: 9082
      name: http
  selector:
    app: rating-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-service
  namespace: neo4flix
  labels:
    app: rating-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rating-service
  template:
    metadata:
      labels:
        app: rating-service
    spec:
      containers:
        - name: rating-service
          image: neo4flix/rating-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9082
              name: http
          envFrom:
            - configMapRef:
                name: neo4flix-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9082
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9082
            initialDelaySeconds: 30
            periodSeconds: 5
